name: schedule test

# A throwaway. Does nothing but prove whether a schedule fires on this
# repository at all. Delete once it has answered the question.
on:
  schedule:
    - cron: "1 14 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "the schedule fired at $(date -u)"
